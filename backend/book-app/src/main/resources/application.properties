# The active profile is injected by Maven resource filtering when running via mvn,
# or can be overridden by SPRING_PROFILES_ACTIVE env var. Default left as placeholder.
spring.profiles.active=@activatedProperties@
spring.jpa.open-in-view=false
